using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectRegion3D : MonoBehaviour 
{

	// Use this for initialization
	void Start () 
    {
        //Material quadMaterial = (Material)Resources.Load("SelectRegion3DMaterial");

        //GetComponent<MeshRenderer>().material = quadMaterial;

        //Texture2D myTexture = Resources.Load("SelectRegion3DTexture") as Texture2D;

        //myTexture.LoadImage();

        //GetComponent<MeshRenderer>().material.mainTexture = myTexture;
	}
	
	// Update is called once per frame
	void Update () 
    {
		
	}
}
